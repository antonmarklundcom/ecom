import "dotenv/config";

// El seed idempotente (categorías, productos, variantes, stock, zonas de
// envío) se implementa en el PR #2 — ver TASKS.md sección 7.
throw new Error("db:seed not implemented yet — see TASKS.md section 7");
