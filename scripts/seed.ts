// Placeholder — real idempotent seed (categorías, productos, variantes,
// stock, zonas de envío) is TASKS.md task 1.12, written once the schema
// (task 1.4) exists.
import "dotenv/config";

async function main() {
  console.log("Seed script not yet implemented — see TASKS.md task 1.12.");
}

main();
